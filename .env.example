# ── Local Docker dev credentials (copy to .env and fill) ──────────────────
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change-me

# mongo-express GUI is at http://localhost:8081 (no login — local dev only)
