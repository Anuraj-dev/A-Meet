# Dependencies
node_modules/
.pnp
.pnp.js

# Env / secrets
.env
.env.local
*.local

# coturn config holds the realm + TURN secret (real values pasted on the server).
# Tracking it caused deploy `git pull` to abort on the server's local edits.
# Track only the placeholder template; the live file is server-local.
coturn/turnserver.conf

# Build output
dist/
build/
client/dist/
client/build/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Test / coverage
coverage/
.nyc_output/
project_files/