# Sessions — 2026-07-06

## 02:55 — Root-caused the 2026-07-05 prod incident (not TURN) + architecture grill
- did: pulled CloudWatch alarm history + `/a-meet/prod/server` logs for both 2026-07-05 alerts.
  `mongo-disconnect` = 97ms Atlas blip, self-healed. `error-rate-5m` = two users (fresh off the
  OAuth redirect) failed `sfu-get-rtp-capabilities` in 237–656ms against a 10s timeout → socket
  disconnect mid-handshake, mislabeled as "timed out", never retried → stuck in-room with no
  media. Grilled Anuraj through recovery contract, error propagation, join sequencing, TURN, and
  the LiveKit question; all resolved (see decisions.md + STATE.md incident block).
- changed: `docs/STATE.md` (incident writeup + agreed fixes), no code changes.
- decided: stay on custom mediasoup; fix the 3 client bugs; cert coturn 5349 then verify via
  `VITE_FORCE_RELAY=1`; new working rule — targeted background reading before new projects/features.
- next: implement the 3 client fixes TDD (retry-on-reconnect after failed first setup; await
  `connect` before setupSfu; propagate real ack errors with socket.connected/elapsed-ms).
