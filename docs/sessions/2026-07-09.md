# 2026-07-09

## 22:31 — Audit initiative: 6 PRDs filed; #159–#161 shipped
- did: investigated the ALARM→OK Telegram flapping (infra transients + no notifier debounce,
  not meetings); ran a full codebase audit (6.3/10 — docs 9, TS 8, a11y/perf 5, public
  readiness 4); filed PRDs #159–#164 with priority order; shipped and merged three of them:
  - PR #165 (#159): `process-down` 5-of-5 + notBreaching, explicit datapoints on
    `instance-health`, `mongo-disconnect` kept 1-of-1; Telegram notifier folds a <10-min
    recovery into one compact line, ALARM timestamp persisted best-effort in SSM.
  - PR #166 (#160): SFU first-connection recovery — SignalError with real causes
    (timeout/disconnect/server, in-flight disconnect tracked), setup awaits socket connect,
    unified runSetup with capped backoff (500ms→8s, 5 tries), reconnecting/retry UI.
  - PR #167 (#161): per-IP express-rate-limit on auth/room routes (60/100 per min), per-ACTOR
    (user id→IP fallback) socket token buckets (signaling 300 burst/100 s⁻¹, chat 20/5) with
    grace-period eviction (10 min) so serial reconnects can't mint fresh buckets;
    retryAfterMs surfaced end-to-end via SignalError.
- changed: deploy/aws-observability.sh, deploy/telegram-notifier/*, deploy/iam-telegram-lambda-policy.json,
  client hooks/services (useMediasoup, mediasoup-signal, RoomPage), server/src/socket/rate-limit.ts (+app.ts,
  handlers, sfu-handlers, env), shared contracts, docs/conventions.md (on main), tests throughout.
- decided: → decisions.md (flap suppression design, mongo-disconnect 1-of-1, actor-keyed buckets).
- process: every PR went Opus coder (isolated worktree) → codex CLI review (caught real bugs each
  round: nonexistent SNS field, SSM hard dependency, timeout misclassification, listener leak,
  per-socket bucket bypass, serial-reconnect bypass) → fix rounds → CI green → squash merge.
- next: #162 (test-coverage gaps: mute-all, ask-to-unmute, pin layout, room E2E), then #163
  (TURN TLS), then #164 (a11y). Raja: `aws login` + re-run deploy/aws-observability.sh + redeploy
  the notifier Lambda to apply #159 to prod.
