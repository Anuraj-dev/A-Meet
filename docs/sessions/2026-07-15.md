# Sessions — 2026-07-15

## 09:59 — Overnight backlog cleanout: every open PR merged, epics ready to close

- did: merged 8 PRs — #156, #172, #86, #173 (rebase-conflict on docker-compose resolved:
  main's healthcheck/limits kept + `restart: always`), #174 (NEW: E2E CI sharding, 3-shard
  SFU-off matrix + blob-report merge job — last #37 criterion), #175 (NEW: deepgram 5 /
  groq 1.x / jsdom 29 migrations + root lockfile sync, supersedes #87/#88/#91), #176 (unified
  Vitest 3→4, supersedes #89/#92/#93), #178 (client minor/patch group with root-lockfile sync
  pushed onto the dependabot branch). Closed superseded dependabot PRs. Zero open PRs remain.
- changed: `.github/workflows/` (sharding), `e2e/playwright.config.js`, `server/src/
  transcription/*` (Deepgram v5 API), `vitest.config.js` both workspaces (coverage floors),
  lockfiles everywhere.
- reviews: codex GPT-5.6 Sol on #174/#175/#176. Two real findings fixed test-first:
  (1) Deepgram v5 start/stop race leaking a socket; (2) coverage-drop justification was wrong —
  real cause is vitest-4 v8 provider counting all branches of untested files.
- orchestration: 2 Opus background agents (vitest-4 finish, #37 sharding) + codex Sol
  (migrations + 4 review rounds) in isolated worktrees; driver did merges/conflicts/lockfiles.
- decided: → decisions.md (coverage floors, dependabot lockfile-sync protocol).
- next: Raja closes epics #31/#33/#35/#37/#38 (agent-blocked); staging sanity check of live
  Deepgram/Groq calls; TURN force-relay + M9.7/M10.12/M12.11 manual verifies.
