# 2026-07-23

## 10:37 — Discord bot: brainstorm → spec → tickets
- did: confirmed zero open issues; brainstormed the Discord bot feature (host identity, trigger
  style, scope, architecture); wrote the approved spec; opened tickets #185 (server integration
  endpoints + linking) and #186 (`bot/` workspace + slash commands, depends on #185).
- changed: `docs/specs/2026-07-23-discord-bot-design.md` (new), `docs/STATE.md`, this log.
- decided: account linking over one-time host tokens; slash commands only; create+link v1 scope;
  separate `bot/` monorepo workspace over HTTP with a bot API key → decisions.md.
- next: fresh-context agent implements #185 (strict TDD, coder/reviewer loop), then #186.

## 13:34 — Discord bot v1 implemented & merged (#185 + #186)
- did: orchestrated two coder/reviewer loops. #185 (PR #187, merged e50fee1): DiscordLink
  model, timing-safe bot-API-key middleware, /api/integrations/discord/{link-token,link,rooms},
  client /link/discord page; codex round 1 caught the key header missing from pino
  REDACT_PATHS (fixed test-first + regression test), round 2 READY TO MERGE. #186 (PR #188,
  merged 50c8e34): bot/ 4th workspace — discord.js slash-only, /meet link (ephemeral) +
  /meet create (public embed, /lobby/<roomId> join URL), typed HTTP client (contracts DTOs,
  10s deadline), defer-before-HTTP + top-level recovery, CI Bot lint/tests, Dockerfile,
  profile-gated compose service; 6 codex rounds, all findings genuine, 35 tests, CI green.
- changed: server/src (integrations, logger redaction), client (link page), shared contracts,
  bot/ (new), .github/workflows, docker-compose.prod.yml, root workspaces/lockfile.
- decided: Joi over spec's "zod" (repo convention); integration routes skip room rate-limiter;
  bot compose service profile-gated with :- defaults → decisions.md.
- next: Raja manual — Discord app/token, invite, DISCORD_BOT_API_KEY both sides, command
  registration; deferred prod bot deploy (SSM /a-meet/prod/bot/*, --profile bot up) per
  bot/README.md.

## 16:05 — Live-test fix (PR #189) + prod bot deploy, verified end-to-end
- did: local test found /meet create embed ephemeral (Discord forces followUp ephemeral
  after ephemeral defer). PR #189: channel.send() public surface + guild-only /meet
  (codex Sol review: 2 rounds, both findings genuine). Fixed prod deploy failures
  (no-space-left on 8GB disk → pruned, rerun green), provisioned SSM bot secrets
  (fresh prod API key), brought up a-meet-bot container on EC2 (--profile bot, on-box
  build from 4054f1a). Raja verified /meet link + /meet create in prod: works.
- changed: bot/src/commands/{handlers,meet}.ts + tests; EC2 box (prune, bot container);
  SSM /a-meet/prod/{bot/*,server/DISCORD_BOT_API_KEY}.
- decided: guild-only /meet; prod bot key distinct from local; AWS account is on the
  credits Free Plan ($58.41 to 2026-11-30) — disk resize decision parked with Raja.
- next: optional ameet EBS resize (16/20GB); optional metaverse docker-root fold-in;
  future: wire bot image into deploy-backend.yml instead of on-box builds.
