# 2026-07-10

## 10:03 — Audit initiative finished: #162/#163/#164 implemented, reviewed, merged
- did: overnight orchestration completed the last three audit PRDs. #162 test-coverage gaps
  (PR #168): server ask-to-unmute-all authz, unpin-restores-layout, E2E for raise-hand /
  fullscreen / layout-chooser / SFU screen share — 3 CI-fix rounds (strict-mode locator
  ambiguity; grid layout's solo invite block hides tiles → switched spec to Spotlight; headless
  Chromium can't synthesize chrome-level Escape → exit via `document.exitFullscreen()`).
  #163 TURN over TLS (PR #169, codex gpt-5.6-terra): `deploy/setup-coturn-tls.sh` +
  lineage-guarded renew hook + nginx ACME vhost + `turns:` ICE URI + `VITE_TURN_TRANSPORT`
  verification filter — 1 review round (5 findings) then approved. #164 room a11y baseline
  (PR #170, Opus coder then codex gpt-5.6-sol after quota/hallucination): aria-pressed
  toggles, `usePanelDialog` focus management, modal mobile sheets, axe-core E2E gate — 1 review
  round (7 findings) + 2 CI rounds (pre-existing unlabeled spinner + contrast debt surfaced by
  the new axe gate; Escape-closes-panel regression vs host-moderation spec). All three merged
  in order after Raja granted merge permission.
- changed: `e2e/tests{,-sfu}/`, `server/test/sfu-handlers-authz.test.js`,
  `client/src/hooks/{useRoomLayout.test.tsx,usePanelDialog.ts}`, `client/src/components/`
  (ControlBar/ChatPanel/PeoplePanel/MediaReconnectingAlert), `client/src/services/ice-config.*`,
  `deploy/`, `coturn/`, `docker-compose.coturn.yml`, README, `docs/conventions.md`.
- decided: Escape-closes-panel dialog semantics with menu-open guard; headless fullscreen-exit
  convention; TURN TLS recovery is operator-rerun not automatic (→ decisions.md).
- also: AWS CLI configured (profile `ameet-prod`); #159 prod-apply runbook written to session
  scratchpad; found `iam-telegram-lambda-policy.json` is missing the live role's `kms:Decrypt`
  (blind apply would break SecureString reads) and `aws-observability.sh` needs uncommitted
  `INSTANCE_ID`/`READINESS_HOST` values.
- next: operator prod applies — #159 observability re-run + Lambda redeploy (per runbook, mind
  the kms:Decrypt gap), then `setup-coturn-tls.sh` on the instance + three-path force-relay
  verification. Then back to M12.11 manual verify.

## 11:15 — Prod applies (#159 obs, #163 TURN TLS) + PR #171
- did: applied #159 observability to prod (IAM policy w/ kms:Decrypt merged back, aws-observability.sh
  with real INSTANCE_ID/READINESS_HOST, all verifies green). Applied #163 TURN TLS on the instance:
  new nginx (ACME for turn.ameet.raja-dev.me), LE cert issued, coturn TLS on 5349. Found live bug —
  certs installed root-only but coturn runs as nobody(65534), TLS silently failed; plus latent hook bug
  (sourced env not exported → first renewal would die). Fixed in PR #171 (codex-reviewed 2 rounds,
  READY TO MERGE, CI green, squash-merged), then pulled+re-ran setup on the instance and verified.
- changed: deploy/setup-coturn-tls.sh, deploy/coturn-renew-hook.sh, deploy/iam-telegram-lambda-policy.json,
  server/test/setup-coturn-tls-script.test.js; prod: nginx site, turnserver.conf, certs, hook, coturn-tls.env.
- decided: cert copies owned by container runtime uid/gid (65534), overridable + persisted → decisions.md.
- next: Raja's manual browser-level three-path force-relay verification; reconcile docs branch with main.
